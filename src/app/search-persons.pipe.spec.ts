import { SearchPersonsPipe } from './search-persons.pipe';

describe('SearchPersonsPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchPersonsPipe();
    expect(pipe).toBeTruthy();
  });
});
